# UI 설계 (ui.md)

모바일 우선, 바닐라, 외부 요청 0. 이 문서에 있는 것만 만든다.

## 1. 레이아웃

* ≥48em: 2열 그리드. 목록:관리탭 = 7:4. 열 사이 간격 18px.
* ≥48em: 전체가 화면을 차지하지 않는다. 좌우 여백은 `max(12px, (100vw - 48rem)/6 + 12px)`로 점점 커진다(48em 밑은 슬라이더 전폭).
* <48em: 상단바 아래가 좌우 2열 트랙. 기본은 목록 열, 헤더 우측 = 아이콘을 탭하면 트랙째 좌측으로 밀리며 우 패널 열이 보인다. 패널이 보이면 = 아이콘은 ×로 바뀐다.
* 상단 헤더: 타이틀 + 경로(`/docs/` 형식 그대로) + 로그아웃 아이콘. 32em 밑에서는 경로가 타이틀 아래 줄에 보인다.

## 2. 추가 버튼 3개 (관리 패널 상단)

`[새 폴더] [파일 업로드] [폴더 업로드]`는 목록 상단이 아니라 **관리 패널 최상단**에 배치. 기록·정보 보기보다 항상 위. <48em에서는 = 탭으로 넘어가면 같은 자리. 목록은 op 후 자동 갱신.

## 3. 목록 행

* 구성: 아이콘 | 이름+메타 1줄(날짜·크기) | ! + ⋯.
* 아이콘은 서버 MIME 판정표(text/plain 목록·extless 이름·기본 mime.types)를 그대로 복제해 종류를 구분한다(추가 요청 없음). text-like application(`js` 등)도 같은 표로 구분한다.
* 탭(클릭)=이동/열기. 정보는 ! 바로가기, 나머지는 ⋯ 메뉴 (전 구간 동일).
* ⋯ 메뉴 항목: `열기` / `정보` / `이름 변경` / `복사` / `이동` / `zip 다운로드`(폴더만) / `삭제`.

## 4. 관리 패널 (기본=기록, 덮어쓰기=정보)

* 기본 모드는 **기록**. 개수 제한 없음. 최근 200개는 localStorage에 영속 저장해서 새로고침·재방문 후에도 남는다(진행 중 항목 제외). 시간 표기는 2026-09-12 09:14:05 형식. 목록 날짜·정보 수정일도 같은 형식이다(수정일은 GMT를 브라우저 현지시간으로 변환). 새 폴더·파일/폴더 올리기(진행+완료)·이름 변경·복사·이동·삭제·zip 다운로드 시작과 각 성공·실패가 쌓이고 최신이 위에 온다. 업로드 중 낱개 실패는 완료 기록에 합산되고, 업로드 완료 기록에는 소요 시간이 함께 붙는다. 항목은 시간 첫 줄, 내용 다음 줄. 대상이 여러 개면 한 줄에 하나씩 나열하고, 4줄이 넘으면 …으로 접어 두었다가 누르면 전체를 본다(다시 누르면 접힘). 목록 조회·폴더 이동·새로고침 성공은 조회일 뿐이라 기록하지 않는다.
* 업로드 중: 기록 항목 안에 progress 바가 살아서 갱신 → 완료 시 완료 기록으로 전환.
* 다운로드는 `location.href` 방식이라 **시작만 기록**한다.
* 기록 맨 아래(가장 오래된 끝)에 「기록 지우기」 버튼. 확인 후 기록 전체를 지운다(서버 파일은 그대로, localStorage도 함께 비운다).
* `!` 또는 ⋯ → `정보`를 누르면 패널이 **정보 전용 보기로 전환**(기록은 뒤에 유지). 「← 기록으로」로 기록 보기로 돌아온다.

## 5. 정보 보기 (실측 확정)

RFC 4918 allprop 이론값과 달리 nginx(dav+dav-ext)가 실제로 주는 것은 아래가 전부다(207 실측). 없는 것은 표시하지 않는다.

* `PROPFIND Depth:0 allprop` → `displayname`(이름) / `getcontentlength`(파일만, 727.5 MB (762,817,299 바이트)처럼 읽기 좋게 표시) / `getlastmodified`(수정일) / `resourcetype`(collection=폴더 판정) / `supportedlock`.
* `HEAD` → `Content-Type`(MIME) / `ETag` / `Accept-Ranges`.
* 목록에서 → 경로.
* `getcontenttype`·`getetag`은 `PROPFIND`에 없어 `HEAD` 값으로 표기한다.
* 항목 아래 미리 보기 (HEAD `Content-Type` 실측 분기, 확장자 목록 없음): `text/html`→sandbox iframe / `image/`→img / `video/`·`audio/`→controls+metadata / `application/pdf`→embed / `text/`·`application/javascript`·`application/json`·`+xml`→전체 pre (매핑 확장자+256KB 이하는 하이라이트, 그 외素). 그 외·로드 실패는 섹션 없음. 상한 없음.
* 실패 시 정보 보기에 에러 1줄.
* 48em부터는 「크게」 버튼으로 정보 보기를 목록 영역까지 넓혀 볼 수 있다(「작게」로 복귀. 「← 기록으로」를 누르면 넓힌 상태가 풀린다).
* 「← 이전」「다음 →」으로 목록 순서대로 이전·다음 항목의 정보를 바로 볼 수 있다(처음·끝은 순환한다).

## 6. 상태 표시

진행 중·완료가 우측 기록에 남는다. 업로드 진행률은 기록 항목 안 progress 바로 표시하고, 중간 실패는 완료 기록에 합산된다.

## 7. <48em 2열 슬라이드

* 헤더 우측의 = 아이콘. 탭하면 상단바 아래 내용이 좌측으로 밀리며(transition) 우측 관리 패널이 보인다. 2열 그리드와 같은 구조다.
* = 아이콘은 기록·정보 보기 모두에서 보이며, 목록과 패널 사이를 오간다.
* 정보 보기 중 패널로 넘어오면 정보 보기가 그대로 보인다(모드 유지).
